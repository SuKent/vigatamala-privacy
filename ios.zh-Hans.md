# Vigatamala 隐私政策

[繁體中文](/ios.zh-Hant) · [简体中文](/ios.zh-Hans) · [English](/ios.en) · [日本語](/ios.ja) · [한국어](/ios.ko)

**最后更新:2026-08-30**
**适用对象:Vigatamala for iOS**

---

## 先说结论

**Vigatamala 不会把你的浏览数据传给我们。**

我们没有账号服务器。App 里没有任何分析、遥测、崩溃报告或广告组件,也没有任何
第三方软件包。你访问过哪些网站、看过什么、搜索过什么,全部只留在你的 iPhone 上。

这份政策的其余部分是把三件事讲清楚:**留在你手机上的到底是什么**、
**App 自己会连到哪里**、**你怎么删掉**。包含几个我们目前还没做好的地方。

---

## 1. 我们收不到的东西

- **我们没有账号后端,也收不到你的浏览数据。** App **自己主动**连向我们域名的,
  只有下载拦截规则列表的那条请求(见第 3 节第 (1) 项)—— 它只带要下载的文件名,
  不带你访问过的网址,也不带账号或设备标识符。
  (设置 → 关于里的“隐私政策”与“拦截规则许可协议”页的镜像链接也指向同一个域名,
  但那是两个**普通的网页链接**:你点了才会前往,和你在任何浏览器里打开一个网址
  没有两样。除此之外没有任何连向我们的连接。)
- **没有任何第三方软件包。** 没有 Google Analytics、Firebase、Crashlytics、
  Sentry,也没有广告联盟。
- **没有广告标识符(IDFA)**,不会弹出“允许跟踪”的请求。
- **不使用定位。** App 没有任何位置权限。
- **地址栏建议完全在设备上生成。** 你打的每一个字都是拿去比对本机的历史与书签。
  多数浏览器会把你的按键发送给搜索引擎换“搜索建议”,我们没有。
- **广告与跟踪器的拦截判断全部在设备上完成。** 我们不会把你要打开的网址发到
  任何服务器问“这个要不要拦”。(iOS 内置的“欺诈性网站警告”是另一回事,
  见第 3 节第 (6) 项 —— 那是系统层功能,对象是 Apple 而不是我们。)

---

## 2. 存在你设备上的数据

以下全部存在 App 的私有文件夹内,**不会发送给我们或任何第三方**。

但要诚实讲一件事:若你开启 iCloud 备份、或用电脑备份 iPhone,这些文件**多数**会随
**系统备份**一并复制。那是 Apple 的备份机制,备份内容我们读不到,也无法读。
我们没有把书签、历史、标签页那些排除在备份之外 —— 排除了你换手机时它们就会消失。

**四份例外**,我们主动排除在备份之外:**规则列表缓存**(随时可以重新下载的
派生数据,没有理由占用你的 iCloud 空间)、**站点图标缓存**(文件名由网站主机
推导得出,没有理由让它离开这台设备)、**标签页缩略图**(那是网页画面本身,
比「去过哪些站」更没有理由离开这台设备)与**诊断日志**(里面有你访问过的
网站主机,理由相同)。

| 数据 | 内容 | 保留 | 怎么删 |
|---|---|---|---|
| 浏览历史 | 完整网址、标题、时间 | 最新 2000 条 | 设置 →“数据”→“清除浏览历史”;或浏览记录页右上角“清除” |
| 标签页 | 网址、标题、自定义名称、固定状态、所属群组(群组名称与颜色) | 无上限 | 关闭标签页,或标签页切换器菜单的“关闭所有标签页”(已固定的会被跳过) |
| 书签 | 网址、标题、加入时间 | 无上限 | 主页书签格逐一移除 |
| 稍后阅读 | 网址、标题、时间(**不存正文**) | 无上限 | 列表页左滑删除;右上角“清除已读” |
| 播放队列 | 媒体网址、标题、表演者、封面网址(**无痕标签页的只留在内存**) | 无上限 | 队列面板左滑删除;关闭该标签页会整份丢掉 |
| 观看进度 | 媒体标识符、播放位置 | 90 天或 500 条 | 随“清除浏览历史”一并清除 |
| 标签页缩略图 | 标签页卡片上显示的页面画面截图(**无痕标签页完全不截取,连内存都没有**) | 最多 60 个文件,超过删最旧 | 关闭该标签页即删;随“清除浏览历史”一并清除;**不进备份** |
| 站点图标缓存 | 你打开过的网站自己声明的小图标,**访问当下**向该站获取(不向第三方、不在后台另外抓取);文件名是主机名的哈希(**无痕标签页不写入文件**) | 最多 200 个文件 | 随“清除浏览历史”一并清除;**不进备份** |
| 偏好设置 | 各项开关;**以及你为个别网站设过的例外** | 无上限 | 该站所有覆盖都改回“沿用全局”时会自动移除 |
| 网站数据 | cookie、缓存、localStorage(由 WebKit 管理,**每个标签页一份独立的**) | 标签页关闭即清除 | 设置 →“数据”→“清除所有网站数据”;或盾牌菜单清除单个站点 |
| 标签页交互状态 | 每个标签页的后退/前进列表(含网址)与滚动位置(**无痕标签页不写文件**) | 跟着标签页走 | 关闭该标签页即删;“清除浏览历史”也会把磁盘上的这份一并清掉 |
| 规则更新记录 | 上次更新时间、规则条数、以及是否走过没有签名的回退路径 | 无上限 | 当前没有删除入口(删除 App 会一并移除) |
| 规则列表缓存 | 下载回来的拦截规则本身(约 14 MB;WebKit 另存一份编译后的产物,约 53 MB),**不含任何你的数据** | 到下次更新覆盖 | 当前没有删除入口(删除 App 会一并移除);**不进备份** |
| 诊断日志 | **默认关闭**。开启后记录功能事件,普通标签页只留网站主机、无痕标签页连主机都不留 | 约 600 KB 封顶,超过只留后半 | 设置 →“隐私”→ 关闭“诊断日志”即**删除整份**;**不进备份** |
| 安装标识符 | 一组随机生成的 UUID,存在 Keychain 而不是文件里。不含任何个人信息 | 无上限 | 见下方第 4 点 —— **删除 App 不保证会移除** |

**要知道的四件事:**

1. **搜索词会以网址的形式留在历史里。** 你在地址栏打字搜索,产生的是一个含
   `?q=你打的字` 的搜索网址,那个网址会被记进浏览历史。清除历史就会一起消失。
2. **“清除浏览历史”不会清掉标签页、书签与稍后阅读。** 那三者各自有自己的删除方式
   (见上表)。
3. **购买记录不在我们这里,我们也没有另外抄一份。** 订阅状态由 Apple 的
   App Store 保管;App 只是向系统问“这个 Apple ID 现在有没有有效订阅”。
   交易编号、扣款日期、剩余天数之类的东西,我们**没有**在你的设备上另存一份,
   也没有服务器可以存。
4. **安装标识符删除 App 之后可能仍然留着,而且会跟着加密备份到新设备。**
   它存在 Keychain 里,而 Apple **没有保证**删除 App 时会一并清除 Keychain
   项目(实际上多半会留下)。我们也刻意没有把它标成“仅限本设备”,所以你从加密
   备份恢复到新 iPhone 时,它会是同一组值 —— 这是为了让换机的你被当成同一个
   安装,而不是凭空多一台设备。它只是一组随机字符串,不含任何个人信息,而且
   **目前没有任何代码会把它发送出设备**。将来若要靠它做账号关联,我们会先更新
   这份政策(见第 8 节)再做。

---

## 3. App 自己会连到哪里

除了你主动浏览的网页之外,App 自己只发出这几种连接:

**(1) 拦截规则列表下载/更新 → `privacy.link2us.link`(回退:`easylist.to`)**
EasyList 与 EasyPrivacy 这两份大型公开列表**不随 App 打包**,要由你自己下载。
默认从我们的镜像 `https://privacy.link2us.link/rules/` 获取已预先转换并签名的
版本(设备端验签);镜像不可用时才回退到上游 `easylist.to` 抓原始列表、
在你的设备上转换。

**什么时候会下载**:首次启动时问你一次(可选“稍后”);设置 →“内容拦截”
里随时可以按“下载列表”。**这两者都不需要订阅。**订阅的是“自动更新规则”
(默认关闭):开启后 App 才会在启动时自动检查更新,最快 24 小时一次。

**这条请求里有什么**:只有要下载的文件名。不含你访问过的网址,不附加任何账号或
设备标识符,并使用独立的网络配置(不带 cookie、不写缓存)。唯一额外的请求头是
`If-None-Match` —— 上次下载时服务器给的文件版本标记,用来在内容没变时省下重抓;
它对应的是那份文件而不是你,拿到同一份列表的人都是同一个值。

**我们这端会收到什么**:这是整份政策里唯一一条连向我们自己域名的请求,所以要
讲清楚。和任何网页服务器一样,连接本身会带着你的 IP 地址、浏览器标识字符串、时间
与请求的文件名。那个域名只是静态文件托管(Cloudflare),我们没有在上面放自己的
代码,**也没有把逐条访问日志导出或保存到任何地方**;我们看得到的只有服务商的
汇总流量统计(总请求数、缓存命中率之类),里面没有个别用户。IP 地址仍会被
服务商在传送与防滥用的过程中处理,那是连接本身无法避免的。回退到上游
`easylist.to` 时,看到你 IP 的是 EasyList 的基础设施(第三方),不在我们手上。

**(2) 锁屏的封面图 → 该网页指定的图片来源**
播放媒体时,锁屏要显示封面。那张图的网址来自网页自己的 metadata,
通常是第三方图床或 CDN。**这是唯一在你没有主动开着页面的情况下也会发生的第三方连接** ——
App 在后台、屏幕锁着时,它仍然会发出。 我们把它限制到最小:使用不带 cookie、不写缓存的独立网络配置,
而且**无痕标签页完全不抓封面图、也不发送任何信息到锁屏**。
要完全停用:设置 →“媒体”→ 关闭“锁屏控制”。
请注意那个开关会一并停用锁屏与控制中心的曲目信息与播放控制。

**(3) 订阅来源(RSS/Atom)→ 你点的那个来源网址**
在你点击地址栏的订阅图标时发生。订阅来源页是一个**普通标签页**,所以你刷新
它、或下次启动 App 恢复该标签页时,也会重新抓一次。使用独立的网络配置,
不带 cookie、不写缓存,内容只在内存解析,不落盘。

**(4) 阅读器界面里的图片 → 文章来源与其图床**
阅读器重排后仍会加载原文的图片。这个界面应用与该标签页相同的拦截规则,
也使用该标签页自己的存储区(所以无痕标签页的阅读器一样不落盘)。

**(5) App 内购买 → Apple(StoreKit)**
只在你购买或恢复购买时发生。往返对象是 Apple,不是我们。
交易记录由 StoreKit(Apple 的系统组件)保管,**我们没有另外抄一份在设备上**,
也没有服务器可以抄 —— App 只是在启动与购买后向系统问一句“这个 Apple ID
现在有没有有效订阅”,拿到的是有或没有,以及到期时间。

**(6) 欺诈性网站警告 → Apple**
iOS 内置的网页引擎会就你访问的网址向 Apple 查询是否为已知的欺诈或恶意网站,
并在命中时拦下。**这是 iOS 的系统层功能,查询对象是 Apple,我们看不到也碰不到。**
我们选择保留它 —— 关掉可以让这份政策少一条,但会让你少一层保护。

**(可选)反馈问题**:设置 → 关于 →“反馈问题”会打开**你自己的**邮件
(或分享面板),收件人是我们的支持邮箱。App 不会自行发送任何数据 ——
正文与附件在发出前全部可见,按下发送的是你。除了你写的描述之外,邮件会
预先填入三行:App 版本、iOS 版本、设备机型(例如“iPhone”)—— 没有序列号、
没有任何标识符,而且你可以在发出前删掉它们。

附件是“诊断日志”(设置 → 隐私,**默认关闭**),只记录功能事件
(播放、拦截、全屏等)。网址在**写入磁盘的当下**就已经遮蔽,不是发出时才处理:

- 普通标签页:只留 `https://主机`,路径与查询字符串一律换成“…”;
- **无痕标签页:连主机都不留**,整个网址换成“‹私密›”;
- 网址里若内嵌了账号密码(`https://账号:密码@…` 这种形式),
  会在遮蔽之前先被去掉。

关闭开关即删除整份日志。我们收到的反馈仅用于调试,不与任何其他数据关联、
不提供给第三方,处理完成后 90 天内删除。

除上述之外,App 不会发出其他对外连接。

---

## 4. 无痕标签页

**做到的:**

- 不写入浏览历史,不写入标签页列表,重开 App 不恢复。
- 网站数据(cookie、缓存)只放在内存,关闭即消失。
- 阅读器使用同一个“只在内存”的存储区。
- 不把标题或封面发送到锁屏、控制中心或车机。
- 播放队列只留在内存,不写入磁盘。

**你主动做的动作仍会留下痕迹:** 在无痕标签页里加书签或加入稍后阅读,
那条网址就会被写进对应的列表。这是刻意的 —— 你明确要求保存的东西,
我们不会偷偷丢掉。但值得知道。

---

## 5. 我们对网页做了什么

Vigatamala 是内容拦截型浏览器。为了达成拦截,我们会:

- 按规则拦截网络请求、隐藏页面元素;
- 拦截页面自己的数据请求以移除广告位(这代表我们会读取并改写网站发来的
  部分响应内容);
- 拦截弹出窗口与返回键劫持;
- 向网站报告经过噪声处理的浏览器指纹(降低你被跨站识别的概率);
- 发送 Global Privacy Control 信号;
- 自动关闭 cookie 同意横幅 —— **一律选择拒绝**。部分网站的同意平台会把这个
  “拒绝”决定上报给它自己的服务器,那是该平台的行为,不是我们发起的;
- 自动关闭“用 App 打开”的推广墙 —— 找得到“暂时不要”这类否定选项时
  **代你按下它**,找不到就只把那层浮层藏起来。**永远不会按推广按钮**,
  含密码框的浮层(登录墙)也不动。同上,被按下的那个选项若让网站记下
  “这个人选了不要”,那是该网站的行为;
- 在发出请求前改写网址:剥除只用于跟踪的参数、把 Google 的 AMP 壳还原成
  原始页面(这一项会换主机,落点是该链接本来就指向的那一页)、
  以及把 http 升级为 https(升级失败不会静默回退,会问你);
- 发送与 Safari 一致的 User-Agent 字符串(让网站不会因为认不出这个浏览器而
  给你阉割版页面);
- 你开启夜间模式时,对浅色网页应用颜色反转。

**除了 User-Agent 之外,以上每一项都可以全局关闭**,多数还能在地址栏的盾牌
菜单针对个别网站关闭(拦截、跟踪器拦截、残留广告位收合、反劫持、指纹防护、
强制 HTTPS、cookie 横幅、夜间模式、桌面版网页)。两个例外要讲清楚:

- **User-Agent 没有关闭选项。** App 一律以与 Safari 一致的字符串自我介绍
  (盾牌菜单的“桌面版网页”只是换成 macOS Safari 的版本,不是关掉它)。
  这是刻意的,有两个理由:一个认不出来的浏览器字符串会让部分网站直接拒绝
  服务或给你阉割版页面;而且**与 Safari 一致的字符串**发出的辨识信息,
  其实比 **WKWebView 的默认字符串更少** —— 它让你看起来和其他所有 Safari
  用户一样,而不是“某个小众浏览器的用户”。
- **Global Privacy Control 只有全局开关**(它是一个对所有网站一致的法律信号,
  逐站决定送不送反而没有意义)。技术限制也一并讲明:`Sec-GPC` 请求头只挂得上
  App 自己发起的主框架请求,iOS 的网页引擎不提供为页面内的子资源请求
  加请求头的途径;`navigator.globalPrivacyControl` 则对整页都成立。

---

## 6. 我们不做的事

- 不销售、不分享、不出租你的任何数据 —— 我们根本没有你的数据。
- 不插入广告、联盟链接或赞助内容。
- 不跟踪你跨 App 或跨网站的行为。
- 不提供内容下载或离线存储功能。

---

## 7. 儿童

本 App 不面向 13 岁以下儿童,也不会在知情的情况下收集儿童的个人信息。

本 App 在 App Store 的年龄分级为 **16+**。这是通用浏览器的必然结果 ——
它能打开任何网页(Apple 问卷里的“不受限的网页访问”),而该项目的最低分级
就是 16+,所有主流浏览器都一样。

---

## 8. 未来的变更

当我们接上账号后端、推出跨设备同步或学生验证时,收集的数据会改变。
届时我们会更新这份政策并在 App 内告知。**目前这些功能都尚未上线。**

---

## 9. 联系我们

有任何疑问请发送至:**vigatamala@link2us.link**

开发者:**Su Shih-neng**
