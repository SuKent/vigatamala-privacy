# Vigatamala プライバシーポリシー

[繁體中文](/ios.zh-Hant) · [简体中文](/ios.zh-Hans) · [English](/ios.en) · [日本語](/ios.ja) · [한국어](/ios.ko)

**最終更新:2026-09-20**
**対象:Vigatamala for iOS**

---

## 結論から先に

**Vigatamala はあなたの閲覧データを当方に送信しません。**

CISIP 対応ビルドは当方のアカウント・サブスクリプションサーバーを利用します（第 3 節）。App には解析、テレメトリ、クラッシュレポート、広告のいずれのコンポーネントも含まれておらず、第三者のソフトウェアパッケージも一切ありません。あなたがどのサイトを訪れたか、何を見たか、何を検索したかは、すべてあなたの iPhone の中だけに残ります。

このポリシーの残りの部分は、次の三つをはっきりさせるためのものです:**あなたの端末に残るものは何か**、**App 自身がどこへ接続するか**、**どうやって削除するか**。当方がまだ十分にできていない点も、いくつか含まれています。

---

## 1. 当方が受け取れないもの

- **閲覧データは当方に送信されません。** App は当方のドメインからルールを取得し、CISIP 対応ビルドでは端末登録とサブスクリプション同期も行います。ルールのリクエストにはアカウント識別子を含めません。アカウントのリクエストは下記の識別情報と取引情報のみです。ポリシーとライセンスのリンクはタップしたときだけ開く通常のページです。
- **第三者のソフトウェアパッケージは一切ありません。** Google Analytics、Firebase、Crashlytics、Sentry もなく、広告ネットワークもありません。
- **広告識別子(IDFA)を使用せず**、「トラッキングを許可」の要求も表示しません。
- **ウェブサイトの位置情報リクエスト（対応ビルド）。** サイトが位置情報を要求すると、iOS と WebKit が許可を求め、ユーザーが許可するかどうかを決めます。要求するのは App 使用中の権限だけで、常時アクセスは要求しません。許可したサイトは位置情報を取得し、そのサイトのプライバシーポリシーに従って扱います。Vigatamala は座標を収集・保存したり、私たちに送信したりしません。iOS の「設定」で App の位置情報へのアクセスを取り消せます。旧ビルドは位置情報の権限を要求しません。
- **アドレスバーの候補はすべて端末上で生成されます。** あなたが打った一文字ごとに照合しているのは、この端末のローカルの履歴とブックマークです。多くのブラウザはあなたの打鍵内容を検索エンジンへ送って「検索候補」と交換しますが、当方はそれをしません。(プライベートタブでは、入力を始めるまで**何も表示しません**。第 4 節をご覧ください。)
- **広告とトラッカーをブロックするかどうかの判断は、すべて端末上で完結します。** あなたが開こうとしている URL を、「これはブロックすべきか」と尋ねるためにどこかのサーバーへ送ることはありません。(iOS 内蔵の「詐欺 Web サイトの警告」は別の話で、第 3 節の (6) を参照してください —— あれはシステム層の機能で、問い合わせの相手は Apple であって当方ではありません。)

---

## 2. あなたの端末に保存されるデータ

表の閲覧データは端末内に残ります。**インストール識別子とサブスクリプション同期データは例外**で、第 3 節に説明します。

ただし、正直に述べておくことが一つあります:iCloud バックアップを有効にしている場合、またはコンピュータで iPhone をバックアップしている場合、これらのファイルの**多く**は**システムバックアップ**とともにコピーされます。それは Apple のバックアップの仕組みであり、バックアップの中身を当方が読むことはできませんし、読む手段もありません。当方はブックマーク、履歴、タブといったものをバックアップから除外していません —— 除外すれば、あなたが機種変更したときにそれらは消えてしまいます。

**四つだけ例外**があり、当方が能動的にバックアップから除外しています:**ルールリストのキャッシュ**(いつでも再ダウンロードできる派生データであり、あなたの iCloud の容量を占める理由がありません)、**サイトアイコンのキャッシュ**(ファイル名がサイトのホスト名から導かれるため、この端末から出す理由がありません)、**タブのサムネイル**(ページの画面そのものであり、「どのサイトを訪れたか」以上にこの端末から出す理由がありません)と**診断ログ**(あなたが訪れたサイトのホスト名が入っており、理由は同じです)。

| データ | 内容 | 保持 | 削除方法 |
|---|---|---|---|
| 閲覧履歴 | 完全な URL、タイトル、日時 | 最新 2000 件 | 設定 →「データ」→「閲覧履歴を消去」;または履歴ページ右上の「消去」 |
| タブ | URL、タイトル、カスタム名、ピン留めの状態、所属グループ(グループ名と色) | 上限なし | タブを閉じる、またはタブ切り替え画面のメニューの「すべてのタブを閉じる」(ピン留めしたものは対象外) |
| ブックマーク | URL、タイトル、追加日時、**プライベートタブから保存したかどうか** | 上限なし | ホームの「ショートカット」の「ブックマーク」→ リストで左スワイプして削除;または右上の「並べ替え」で編集モードにしてから削除 |
| リーディングリスト | URL、タイトル、日時(**本文は保存しません**) | 上限なし | リストのページで左スワイプして削除;右上の「既読を消去」 |
| 再生キュー | メディアの URL、タイトル、アーティスト、アートワークの URL(**プライベートタブのものはメモリ上にしか残りません**) | 上限なし | キューのパネルで左スワイプして削除;そのタブを閉じると丸ごと破棄されます |
| 再生位置 | メディアの識別子、再生位置 | 90 日または 500 件 | 「閲覧履歴を消去」とあわせて消去されます |
| タブのサムネイル | タブのカードに表示されるページ画面のスクリーンショット(**プライベートタブは取得自体を行いません。メモリ上にも残りません**) | 最大 60 ファイル。超えたら古いものから削除 | そのタブを閉じると削除されます;「閲覧履歴を消去」とあわせて消去されます;**バックアップに入りません** |
| サイトアイコンのキャッシュ | 開いたサイトが自ら宣言している小さなアイコンを、**訪問したその場で**そのサイト自身から取得します(第三者からは取得せず、バックグラウンドでも取得しません)。ファイル名はホスト名のハッシュ(**プライベートタブは書き込みません**) | 最大 200 ファイル | 「閲覧履歴を消去」とあわせて消去されます;**バックアップに入りません** |
| 環境設定 | 各種のスイッチ;**および個別のサイトに設定した例外** | 上限なし | そのサイトのすべての上書きを「全体設定に従う」に戻すと自動的に削除されます |
| サイトデータ | cookie、キャッシュ、localStorage(WebKit が管理し、**タブごとに独立した一つ**) | タブを閉じると消去 | 設定 →「データ」→「すべてのサイトデータを消去」;またはシールドメニューから個別のサイトを消去 |
| タブの操作状態 | タブごとの戻る/進むのリスト(URL を含む)とスクロール位置(**プライベートタブはファイルに書きません**) | タブと同じだけ | そのタブを閉じれば削除;「閲覧履歴を消去」でもディスク上のこの分がまとめて消去されます |
| ルール更新の記録 | 前回の更新日時、ルールの件数、および署名のない代替経路を通ったかどうか | 上限なし | 現時点で削除の入口はありません(App を削除すればあわせて除去されます) |
| ルールリストのキャッシュ | ダウンロードしたブロックルールそのもの(約 14 MB;WebKit がコンパイル後の成果物を別途約 53 MB 保持します)、**あなたのデータは一切含みません** | 次回の更新で上書き | 現時点で削除の入口はありません(App を削除すればあわせて除去されます);**バックアップに入りません** |
| 診断ログ | **初期設定はオフ**。オフのときは今回の起動分の直近 100 件だけがメモリに残り、サイトのホスト名も残しません。オンにするとファイルに書き出し、通常のタブではサイトのホスト名を残し、プライベートタブではホスト名すら残しません | メモリの分は最大 100 件、App を閉じると消えます。ファイルは約 600 KB を上限とし、超えた場合は後半のみを保持 | 設定 →「プライバシー」→「診断ログ」をオフにすると**両方とも削除**;**バックアップに入りません** |
| インストール識別子 | ランダムに生成された UUID 一つ。ファイルではなく Keychain に保存されます。名前やメールは含みませんが購入を関連付けます | 上限なし | 下記の 4 番目を参照 —— **App を削除しても除去される保証はありません** |

**知っておくべき四つのこと:**

1. **検索した語は URL の形で履歴に残ります。** アドレスバーに入力して検索すると、`?q=あなたが打った語` を含む検索 URL が生成され、その URL が閲覧履歴に記録されます。履歴を消去すればあわせて消えます。
2. **「閲覧履歴を消去」ではタブ、ブックマーク、リーディングリストは消えません。** その三つにはそれぞれ独自の削除方法があります(上の表を参照)。
3. **取引は Apple が保持し、CISIP にも同期されます。** App は送信待ちの署名付き取引を再試行用に保存し、成功後に削除します。このファイルはバックアップ対象外です。サーバーは購入確認、復元、返金処理のため端末・アカウントの対応、契約と取引を保存します。
4. **インストール UUID とセッショントークンは Keychain に保存されます。** 起動、閲覧データ消去、再インストールによって App が意図的に UUID を変更することはありません。ただし Apple は削除後の Keychain の保持を保証せず、端末消去などで失われる場合があります。暗号化バックアップで別端末へ引き継がれる場合があります。名前やメールは含みませんが、端末アカウントと購入を関連付ける識別子であり、関連付け不能な匿名情報ではありません。


---

## 3. App 自身がどこへ接続するか

あなたが自分で閲覧する Web ページを除けば、App 自身が発するのは次の種類の接続だけです:

**(1) ブロックルールリストのダウンロード/更新 → `privacy.link2us.link`(代替経路:`easylist.to`)**
EasyList と EasyPrivacy というこの二つの大規模な公開リストは**App に同梱されておらず**、あなた自身でダウンロードする必要があります。既定では当方のミラー `https://privacy.link2us.link/rules/` から、あらかじめ変換して署名した版を取得します(署名は端末側で検証します);ミラーが利用できないときにだけ、上流の `easylist.to` へ戻って元のリストを取得し、あなたの端末上で変換します。

**いつダウンロードするか**:初回起動時に一度だけ尋ねます(「あとで」を選べます);設定 →「コンテンツブロック」ではいつでも「リストをダウンロード」を押せます。**このどちらにもサブスクリプションは不要です。** サブスクリプションで購入するのは「ルールの自動更新」(初期設定はオフ)で、オンにして初めて App が起動時に自動で更新を確認するようになり、最短で 24 時間に一度です。

**このリクエストに含まれるもの**:ダウンロードするファイル名だけです。あなたが訪れた URL は含まず、アカウントや端末の識別子も付加せず、独立したネットワーク構成(cookie を持たず、キャッシュを書きません)を使います。唯一の追加のヘッダは `If-None-Match` です —— 前回のダウンロードのときにサーバーが渡したファイルのバージョン標識で、内容が変わっていないときに取り直しを省くために使います;それが対応しているのはそのファイルであってあなたではなく、同じリストを取得した人はみな同じ値です。

**当方の側に届くもの**:どんな Web サーバーとも同じく、接続そのものがあなたの IP アドレス、ブラウザの識別文字列、時刻、そしてリクエストされたファイル名を伴います。そのドメインは静的ファイルのホスティング(Cloudflare)にすぎず、当方はその上に自前のコードを置いておらず、**一件ごとのアクセスログをどこかへ書き出したり保存したりもしていません**;当方が見られるのは事業者側の集計されたトラフィック統計(総リクエスト数、キャッシュヒット率など)だけで、そこに個々の利用者は含まれません。IP アドレスは配信と不正利用の防止の過程で事業者になお処理されますが、それは接続そのものに避けられないことです。上流の `easylist.to` へ戻ったときにあなたの IP を見るのは EasyList のインフラ(第三者)であり、当方の手にはありません。

**(2) ロック画面のアートワーク → その Web ページが指定した画像の取得元**
メディアを再生するとき、ロック画面はアートワークを表示します。その画像の URL は Web ページ自身の metadata に由来し、多くは第三者の画像ホストや CDN です。**これは、あなたが能動的にページを開いていない状況でも発生する唯一の第三者接続です** —— App がバックグラウンドにあり、画面がロックされていても、それはなお送出されます。 当方はそれを最小限に抑えています:cookie を持たず、キャッシュを書かない独立したネットワーク構成を使い、しかも**プライベートタブではアートワークを一切取得せず、ロック画面へどんな情報も送りません**。
完全に無効にするには:設定 →「メディア」→「ロック画面に曲目を表示」をオフにします(1.1 以前のバージョンでは「ロック画面のコントロール」)。
オフにすると、ロック画面・コントロールセンター・車載ディスプレイに当方が送る曲目情報とアートワークは表示されません。再生とその操作には影響しません。

**(3) フィード(RSS/Atom)→ あなたがタップしたそのフィードの URL**
あなたがアドレスバーのフィードのアイコンをタップしたときに発生します。フィードのページは**実体のあるタブ**なので、あなたがそれを再読み込みしたとき、あるいは次回 App を起動してそのタブが復元されるときにも、もう一度取得されます。独立したネットワーク構成を使い、cookie を持たず、キャッシュを書かず、内容はメモリ上でのみ解析され、ディスクには落ちません。**プライベートタブから開いた場合は、開かれるタブもプライベートタブです** —— そのアドレスには今見ているサイトが埋め込まれており、通常のタブで開けば閲覧履歴に書き込むことになります。

**(4) リーダー画面の中の画像 → 記事の取得元とその画像ホスト**
リーダーで整形したあとも元記事の画像はなお読み込まれます。この画面はそのタブと同じブロックルールを適用し、そのタブ自身のストレージも使います(そのためプライベートタブのリーダーも同様にディスクに残しません)。

**(5) App 内課金 → Apple(StoreKit)**
購入と復元は Apple StoreKit が処理します。App 内購入には CISIP 登録と同じインストール UUID を appAccountToken として付け、Apple が取引に保持します。起動、フォアグラウンド復帰、取引更新時にも契約情報を確認し、下記の範囲で同期します。


**(6) 詐欺 Web サイトの警告 → Apple**
iOS 内蔵の Web エンジンが、あなたの訪れる URL について既知の詐欺サイトや悪意のあるサイトかどうかを Apple に問い合わせ、該当したときにブロックします。**これは iOS のシステム層の機能で、問い合わせの相手は Apple であり、当方には見ることも触れることもできません。** 当方はそれを残す選択をしました —— オフにすればこのポリシーは一項目短くなりますが、あなたの保護は一層薄くなります。

**(任意)問題の報告**:設定 →「このAppについて」→「問題を報告」は、**あなた自身の**メール(または共有シート)を開き、宛先は当方のサポート用のメールアドレスです。App が自ら何かのデータを送信することはありません —— 本文と添付は送信前にすべて見えており、送信を押すのはあなたです。あなたが書いた説明のほかに、メールには四行が事前に入力されます:App のバージョン、ビルド日時、iOS のバージョン、端末の機種(例:「iPhone」)—— シリアル番号はなく、いかなる識別子もなく、しかもあなたは送信前にそれらを削除できます。

メールには**診断内容**が添えられ、機能イベント(再生、ブロック、フルスクリーンなど)だけを記録します。設定 → プライバシー →「診断ログ」(**初期設定はオフ**)をオンにしているかどうかで、二つの形態があります:

**オフのとき(初期設定)**:ファイルには一切書き込みません。**今回の起動分**の直近 100 件だけがメモリに残り、報告の際に少なくとも「何が起きたか」を添えられるようにします。この内容は**サイトのホスト名すら残しません** —— URL はすべて「‹網址›」に置き換わり、残るのはイベント名だけです。ディスクに書き込まれず、バックアップにも入らず、App を閉じれば消え、あなたが報告を送ったときにだけ端末を離れます。

**オンのとき**:イベントは端末上のログファイル(約 600 KB を上限とし、超えた場合は後半のみを保持)に書き出され、添付または本文として送られます。URL は**ディスクへ書き込むまさにその時点で**すでに伏せられており、送信するときに処理するのではありません:

- 通常のタブ:`https://ホスト名` だけを残し、パスとクエリ文字列は一律「…」に置き換えます;
- **プライベートタブ:ホスト名すら残さず**、URL 全体を「‹私密›」に置き換えます;
- URL の中にアカウントとパスワードが埋め込まれている場合(`https://アカウント:パスワード@…` という形式)、伏せる処理の前にまず取り除かれます。

スイッチをオフにするとログファイルは全体が削除され、メモリ上の分も一緒に消えます。

⚠️ 「共有する」または「Gmail で開く」を押すと、内容全体が**システムのクリップボード**にもコピーされます —— メールに直接貼り付けられるようにするためです(Gmail の場合、iOS は App がファイルを直接添付することを許していません)。iOS では他の App もクリップボードを読めます。別のものをコピーすれば上書きされます。

当方が受け取った報告はデバッグにのみ使用し、ほかのいかなるデータとも関連付けず、第三者に提供せず、対応が完了してから 90 日以内に削除します。

上記のほかに、App が外部へ接続を発することはありません。

**(7) 端末アカウントと契約の同期 → `apple.link2us.link`（CISIP 対応ビルド）**
起動時やフォアグラウンド復帰時に、インストール UUID、App bundle ID、プラットフォーム ios を HTTPS で送信し、端末アカウントを作成・復元します。以後は Keychain のセッショントークンを使います。Plus の取引がある場合は取引番号、元取引番号、製品 ID、本番／サンドボックス環境、既存の appAccountToken、Apple 署名付き取引を送信します。サーバーは Apple に確認し、必要に応じて App 外で引き換えた取引にトークンを付け、契約を再照会します。**閲覧履歴、URL、検索、ブックマーク、再生内容、位置情報は送信しません。** 接続サービスは IP など必要な接続情報を処理します。用途はアカウントと契約の管理で、広告追跡ではありません。App や閲覧データを削除してもサーバー記録は消えません。照会・削除は第 9 節の連絡先へご依頼ください。**削除では端末の紐づけ、権利と契約の状態、学生確認の記録を消去します。ただし二つは一緒には消えません。一つは端末そのものの記録(インストール識別子を含む)で、アカウントを削除しても残り、90 日間活動がなければ自動的に削除します(不正利用の防止に必要な場合を除きます)。もう一つは取引記録と Apple 署名付き通知そのもので、あなたとの紐づけだけを外して保持します**——返金と購入紛争に対応するための証跡だからです。アカウントと取引はサービス提供と購入紛争の対応に必要な期間保持します。端末の利用状況の記録は既定で 180 日です。未対応の旧ビルドはこの通信を行いません。


---

## 4. プライベートタブ

**できていること:**

- 閲覧履歴に書き込まず、タブのリストにも書き込まず、App を開き直しても復元しません。
- サイトデータ(cookie、キャッシュ)はメモリ上にのみ置かれ、閉じれば消えます。
- リーダーも同じ「メモリ上のみ」のストレージを使います。
- タイトルやアートワークをロック画面、コントロールセンター、車載システムへ送りません。
- 再生キューはメモリ上にのみ残り、ディスクには書き込みません。
- **アドレスバーは、入力を始めるまで候補を表示しません。** 通常のタブではアドレスバーにフォーカスするとよく行く場所(端末内の履歴とブックマークから)を並べますが、プライベートタブでは並べません —— それはアドレスバーに触れただけで、よく見るサイトの一覧を隣の人に見せることになります。入力を始めれば照合します(すべて端末内で計算)。その部分も止めたい場合は、設定 →「プライバシー」の「プライベートタブでローカル候補を使わない」があります。
- **プライベートタブが開いている間は、マルチタスク画面のプレビューを覆います。** iOS は App がバックグラウンドに移るとき独自に全画面のスナップショットを撮ります。その前に画面を覆います。

**あなたが能動的に行った操作はなお痕跡を残します:** プライベートタブでブックマークに追加したり、リーディングリストに追加したりすると、その URL は対応するリストに書き込まれます。これは意図的です —— あなたが明示的に保存を求めたものを、当方が黙って捨てることはしません。ただし知っておく価値のあることです。

**ただし、プライベートタブから保存したことを覚えていて、開き直すときもプライベートタブで開きます。** そうでなければ、ブックマークを一度タップするだけでその URL が閲覧履歴に入り、cookie も通常のストレージに入ります —— あなたは何も選び直していないのに。(2026-09-07 以降。それ以前に保存したブックマークにはこの情報がなく、通常のタブ由来として扱います。)

---

## 5. 当方が Web ページに対して行っていること

Vigatamala はコンテンツブロック型のブラウザです。ブロックを実現するために、当方は次のことを行います:

- ルールに従ってネットワークリクエストをブロックし、ページの要素を隠します;
- 広告枠を取り除くためにページ自身のデータリクエストを横取りします(これは、サイトが送ってくる応答内容の一部を当方が読み取り、書き換えることを意味します);
- ポップアップと戻るボタンのハイジャックを遮ります;
- ノイズを加えたブラウザのフィンガープリントをサイトに報告します(あなたがサイトをまたいで特定される可能性を低くします);
- Global Privacy Control の信号を送出します;
- cookie 同意バナーを自動的に閉じます —— **一律に拒否を選びます**。一部のサイトの同意プラットフォームは、この「拒否」という決定を自身のサーバーへ報告することがありますが、それはそのプラットフォームの動作であって、当方が起こしたものではありません;
- 「App で開く」という勧誘の壁を自動的に閉じます —— 「今はしない」のような否定の選択肢が見つかるときは**あなたに代わってそれを押し**、見つからないときはそのオーバーレイを隠すだけにします。**勧誘のボタンを押すことは決してありません**し、パスワード欄を含むオーバーレイ(ログインの壁)にも手を触れません。同じく、押されたその選択肢によってサイトが「この人は不要を選んだ」と記録するのであれば、それはそのサイトの動作です;
- リクエストを送る前に URL を書き換えます:トラッキングのためだけのパラメータを取り除き、Google の AMP の殻を元のページへ戻し(この項目はホストが変わりますが、行き着く先はそのリンクがもともと指していたそのページです)、そして http を https に格上げします(格上げに失敗したときは黙って戻さず、あなたに尋ねます);
- Safari と一致する User-Agent 文字列を送出します(そのブラウザを認識できないという理由で、サイトがあなたに機能を削った版のページを返さないようにするためです);
- あなたがナイトモードをオンにしたとき、明るい配色の Web ページに色の反転を適用します。

**User-Agent を除けば、以上の一つひとつはすべて全体でオフにできます**。多くはさらに、アドレスバーのシールドメニューから個別のサイトごとにオフにできます(ブロック、トラッカーをブロック、残った広告枠を除去、ハイジャック防止、フィンガープリント保護、HTTPS を強制、Cookie 通知、ナイトモード、デスクトップ用サイト)。二つの例外をはっきり述べておきます:

- **User-Agent にはオフにする選択肢がありません。** App は一律に Safari と一致する文字列で自己紹介します(シールドメニューの「デスクトップ用サイト」は macOS Safari の版に差し替えるだけで、それをオフにするものではありません)。これは意図的です:認識されないブラウザ文字列は、一部のサイトからサービスをそのまま拒否されたり、機能を削った版のページを返されたりする原因になりますし、そして **Safari と一致する文字列**が送り出す識別情報は、実のところ **WKWebView の既定の文字列よりも少ない**のです —— それはあなたを、「あるニッチなブラウザの利用者」ではなく、ほかのすべての Safari 利用者と同じに見せます。
- **Global Privacy Control には全体のスイッチしかありません**(それはすべてのサイトに対して一貫した法的な信号であり、サイトごとに送る送らないを切り替えることには、かえって意味がありません)。技術的な制限もあわせて明記します:`Sec-GPC` ヘッダは App 自身が発するメインフレームのリクエストにしか付けられず、iOS の Web エンジンはページ内のサブリソースのリクエストにヘッダを追加する手段を提供していません;`navigator.globalPrivacyControl` のほうはページ全体に対して成立します。

---

## 6. 当方が行わないこと

- あなたのいかなるデータも販売せず、共有せず、貸与しません —— そもそも当方はあなたのデータを持っていません。
- 広告、アフィリエイトリンク、スポンサードコンテンツを挿入しません。
- App をまたいだ、あるいはサイトをまたいだあなたの行動を追跡しません。
- コンテンツのダウンロードやオフライン保存の機能を提供しません。

---

## 7. 子どもについて

本 App は 13 歳未満の子どもを対象としておらず、子どもの個人情報を知りながら収集することもありません。

本 App の App Store での年齢制限は **16+** です。これは汎用ブラウザであることの必然的な結果です —— それは任意の Web ページを開けるものであり(Apple の質問票でいう「制限のない Web アクセス」)、その項目の最低の年齢制限がまさに 16+ で、主要なブラウザはすべて同じです。

---

## 8. 今後の変更

今回、端末アカウントと契約同期を追加しました。閲覧データの端末間同期と学生認証は App では未提供です。新しい利用目的を追加するときは本ポリシーを更新し、App 内でお知らせします。


---

## 9. お問い合わせ

ご不明な点は次までお送りください:**vigatamala@link2us.link**

開発者:**Su Shih-neng**
