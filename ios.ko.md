# Vigatamala 개인정보 처리방침

[繁體中文](/ios.zh-Hant) · [简体中文](/ios.zh-Hans) · [English](/ios.en) · [日本語](/ios.ja) · [한국어](/ios.ko)

**최종 수정일: 2026-08-30**
**적용 대상: Vigatamala for iOS**

---

## 결론부터

**Vigatamala는 사용자의 브라우징 데이터를 저희에게 보내지 않습니다.**

저희에게는 계정 서버가 없습니다. 앱에는 분석, 텔레메트리, 크래시 리포트, 광고 관련 구성 요소가 전혀 없고, 어떤 종류의 서드파티 소프트웨어 패키지도 들어 있지 않습니다. 어떤 사이트를 방문했는지, 무엇을 보았는지, 무엇을 검색했는지는 전부 사용자의 iPhone 안에만 남습니다.

이 처리방침의 나머지 부분은 세 가지를 분명히 밝히기 위한 것입니다. **사용자의 기기에 남는 것이 정확히 무엇인지**, **앱 자체가 어디에 연결하는지**, **어떻게 지우는지**. 여기에는 저희가 아직 제대로 해내지 못한 부분 몇 가지도 포함되어 있습니다.

---

## 1. 저희가 받을 수 없는 것

- **저희에게는 계정 백엔드가 없으며, 사용자의 브라우징 데이터를 받을 수도 없습니다.** 앱이 **스스로 먼저** 저희 도메인에 연결하는 것은 차단 규칙 목록을 내려받는 요청 하나뿐입니다(3절 (1) 항목 참조). 그 요청에는 내려받을 파일 이름만 들어 있으며, 방문한 URL도, 계정이나 기기 식별자도 들어 있지 않습니다. (설정 → 정보에 있는 ‘개인정보 처리방침’ 링크와 ‘차단 규칙 라이선스’ 페이지의 미러 링크도 같은 도메인을 가리키지만, 그 둘은 **평범한 웹 링크**입니다. 탭해야 비로소 이동하며, 어떤 브라우저에서든 URL 하나를 여는 것과 다르지 않습니다. 그 밖에 저희 쪽으로 향하는 연결은 없습니다.)
- **어떤 서드파티 소프트웨어 패키지도 없습니다.** Google Analytics, Firebase, Crashlytics, Sentry도 없고, 광고 네트워크도 없습니다.
- **광고 식별자(IDFA)가 없으며**, ‘추적 허용’ 요청 창도 뜨지 않습니다.
- **위치 정보를 사용하지 않습니다.** 앱에는 어떤 위치 권한도 없습니다.
- **주소 표시줄의 제안은 전적으로 기기 안에서 만들어집니다.** 입력하는 글자는 기기에 저장된 방문 기록과 북마크를 대조하는 데에만 쓰입니다. 대부분의 브라우저는 입력한 키를 검색 엔진에 보내 ‘검색 제안’을 받아 오지만, 저희는 그렇게 하지 않습니다.
- **광고와 트래커의 차단 판단은 전부 기기 안에서 이루어집니다.** 사용자가 열려는 URL을 어떤 서버에 보내 ‘이건 차단할까요’라고 묻는 일은 없습니다. (iOS에 내장된 ‘사기 웹사이트 경고’는 별개의 사안입니다. 3절 (6) 항목을 참조하세요 — 그것은 시스템 차원의 기능이며, 조회 대상은 Apple이지 저희가 아닙니다.)

---

## 2. 사용자의 기기에 저장되는 데이터

아래 항목은 모두 앱의 비공개 폴더 안에 저장되며, **저희나 어떤 제3자에게도 전송되지 않습니다**.

다만 한 가지는 솔직히 말씀드려야 합니다. iCloud 백업을 켜 두었거나 컴퓨터로 iPhone을 백업하면, 이 파일들의 **대부분**은 **시스템 백업**과 함께 복사됩니다. 그것은 Apple의 백업 메커니즘이며, 백업 내용을 저희는 읽을 수 없고 읽을 방법도 없습니다. 저희는 북마크, 방문 기록, 탭 같은 것들을 백업에서 제외하지 않았습니다 — 제외하면 기기를 바꿀 때 그것들이 사라지기 때문입니다.

**두 가지는 예외로**, 저희가 직접 백업에서 제외했습니다. **규칙 목록 캐시**(언제든 다시 내려받을 수 있는 파생 데이터이므로 사용자의 iCloud 용량을 차지할 이유가 없습니다)와 **진단 로그**(그 안에는 방문한 사이트의 호스트가 들어 있으므로 이 기기를 떠날 이유가 없습니다)입니다.

| 데이터 | 내용 | 보존 | 삭제 방법 |
|---|---|---|---|
| 방문 기록 | 전체 URL, 제목, 시각 | 최신 2,000건 | 설정 → ‘데이터’ → ‘방문 기록 지우기’, 또는 방문 기록 화면 오른쪽 위의 ‘지우기’ |
| 탭 | URL, 제목, 사용자가 지정한 이름, 고정 여부 | 제한 없음 | 탭을 닫거나, 탭 전환기 메뉴의 ‘모든 탭 닫기’(고정된 탭은 제외됩니다) |
| 북마크 | URL, 제목, 추가한 시각 | 제한 없음 | 홈 화면의 북마크 칸에서 하나씩 삭제 |
| 읽기 목록 | URL, 제목, 시각(**본문은 저장하지 않습니다**) | 제한 없음 | 목록 화면에서 왼쪽으로 밀어 삭제, 오른쪽 위의 ‘읽은 항목 지우기’ |
| 재생 대기열 | 미디어 URL, 제목, 아티스트, 아트워크 URL(**비공개 탭의 것은 메모리에만 남습니다**) | 제한 없음 | 대기열 패널에서 왼쪽으로 밀어 삭제, 해당 탭을 닫으면 전체가 버려집니다 |
| 재생 위치(이어보기) | 미디어 식별자, 재생 위치 | 90일 또는 500건 | ‘방문 기록 지우기’와 함께 지워집니다 |
| 환경설정 | 각 항목의 설정 값, **그리고 개별 사이트에 지정한 예외** | 제한 없음 | 해당 사이트의 모든 개별 설정을 ‘전역 설정 따름’으로 되돌리면 자동으로 삭제됩니다 |
| 웹사이트 데이터 | cookie, 캐시, localStorage(WebKit이 관리하며, **탭마다 각각 별개로 하나씩**) | 탭을 닫으면 즉시 삭제 | 설정 → ‘데이터’ → ‘모든 웹사이트 데이터 지우기’, 또는 방패 메뉴에서 개별 사이트만 지우기 |
| 탭 상호작용 상태 | 탭별 뒤로/앞으로 목록(URL 포함)과 스크롤 위치(**비공개 탭은 파일로 쓰지 않습니다**) | 해당 탭을 따라갑니다 | 해당 탭을 닫으면 삭제되며, ‘방문 기록 지우기’도 디스크에 남아 있는 이 데이터를 함께 지웁니다 |
| 규칙 업데이트 기록 | 마지막 업데이트 시각, 규칙 개수, 그리고 서명 없는 대체 경로를 거쳤는지 여부 | 제한 없음 | 현재 삭제 수단이 없습니다(앱을 삭제하면 함께 제거됩니다) |
| 규칙 목록 캐시 | 내려받은 차단 규칙 자체(약 14 MB. WebKit이 컴파일한 결과물을 약 53 MB 별도로 보관합니다). **사용자의 데이터는 전혀 포함되지 않습니다** | 다음 업데이트 때 덮어씁니다 | 현재 삭제 수단이 없습니다(앱을 삭제하면 함께 제거됩니다). **백업에 포함되지 않습니다** |
| 진단 로그 | **기본값은 꺼짐입니다.** 켜면 기능 이벤트를 기록하며, 일반 탭은 사이트 호스트만 남기고 비공개 탭은 호스트조차 남기지 않습니다 | 약 600 KB 상한, 넘으면 뒤쪽 절반만 남깁니다 | 설정 → ‘개인정보 보호’ → ‘진단 로그’를 끄면 **전체가 삭제됩니다**. **백업에 포함되지 않습니다** |
| 설치 식별자 | 무작위로 생성된 UUID 하나. 파일이 아니라 Keychain에 저장됩니다. 어떤 개인정보도 포함하지 않습니다 | 제한 없음 | 아래 4번 항목 참조 — **앱을 삭제해도 제거된다는 보장이 없습니다** |

**알아 두셔야 할 네 가지:**

1. **검색어는 URL의 형태로 방문 기록에 남습니다.** 주소 표시줄에 입력해 검색하면 `?q=입력한 글자`를 포함한 검색 URL이 만들어지고, 그 URL이 방문 기록에 기록됩니다. 방문 기록을 지우면 함께 사라집니다.
2. **‘방문 기록 지우기’는 탭, 북마크, 읽기 목록을 지우지 않습니다.** 그 세 가지에는 각각 별도의 삭제 방법이 있습니다(위 표 참조).
3. **구매 기록은 저희에게 없으며, 저희가 따로 복사해 두지도 않습니다.** 구독 상태는 Apple의 App Store가 보관합니다. 앱은 시스템에 ‘이 Apple ID에 지금 유효한 구독이 있는가’를 물을 뿐입니다. 거래 번호, 결제일, 남은 일수 같은 것을 저희가 사용자의 기기에 따로 저장하는 일은 **없으며**, 저장할 수 있는 서버도 없습니다.
4. **설치 식별자는 앱을 삭제한 뒤에도 남아 있을 수 있고, 암호화 백업을 따라 새 기기로 옮겨집니다.** 이 값은 Keychain에 저장되어 있는데, Apple은 앱을 삭제할 때 Keychain 항목이 함께 지워진다고 **보장하지 않습니다**(실제로는 대체로 남습니다). 저희도 의도적으로 이 값을 ‘이 기기 전용’으로 표시하지 않았습니다. 그래서 암호화 백업에서 새 iPhone으로 복원하면 같은 값이 됩니다 — 기기를 바꾼 사용자를 새로 생긴 기기가 아니라 같은 설치로 취급하기 위해서입니다. 이 값은 무작위 문자열일 뿐 어떤 개인정보도 포함하지 않으며, **현재 이 값을 기기 밖으로 내보내는 코드는 전혀 없습니다**. 앞으로 이 값을 계정 연결에 사용하게 된다면, 그 전에 이 처리방침을 먼저 갱신하고 나서 진행하겠습니다(8절 참조).

---

## 3. 앱 자체가 연결하는 곳

사용자가 직접 방문하는 웹페이지를 제외하면, 앱이 스스로 만드는 연결은 다음 몇 가지뿐입니다.

**(1) 차단 규칙 목록 다운로드/업데이트 → `privacy.link2us.link`(대체 경로: `easylist.to`)**
EasyList와 EasyPrivacy라는 두 개의 대용량 공개 목록은 **앱에 함께 포함되어 있지 않으며**, 사용자가 직접 내려받아야 합니다. 기본적으로는 저희 미러인 `https://privacy.link2us.link/rules/`에서 미리 변환하고 서명한 버전을 받습니다(서명은 기기에서 검증합니다). 미러를 사용할 수 없을 때에만 상위 원본인 `easylist.to`로 되돌아가 원본 목록을 받아 사용자의 기기에서 변환합니다.

**언제 내려받는가**: 첫 실행 때 한 번 물어봅니다(‘나중에’를 선택할 수 있습니다). 설정 → ‘콘텐츠 차단’에서 언제든 ‘목록 다운로드’를 누를 수 있습니다. **이 둘 모두 구독이 필요하지 않습니다.** 구독의 대상은 ‘규칙 자동 업데이트’(기본값은 꺼짐)입니다. 켜야만 앱이 실행 시 자동으로 업데이트를 확인하며, 확인 빈도는 24시간에 한 번을 넘지 않습니다.

**이 요청에 들어 있는 것**: 내려받을 파일 이름뿐입니다. 방문한 URL은 포함되지 않고, 어떤 계정이나 기기 식별자도 덧붙이지 않으며, 독립된 네트워크 설정(cookie를 싣지 않고 캐시를 쓰지 않음)을 사용합니다. 추가로 붙는 헤더는 `If-None-Match` 하나뿐입니다 — 지난번 다운로드 때 서버가 준 파일 버전 표식으로, 내용이 바뀌지 않았을 때 다시 받는 것을 아끼기 위한 것입니다. 이 값은 사용자가 아니라 그 파일에 대응하며, 같은 목록을 받은 사람은 모두 같은 값을 가집니다.

**저희 쪽에 도달하는 것**: 이것은 이 처리방침을 통틀어 저희 자신의 도메인으로 향하는 유일한 요청이므로 분명히 밝히겠습니다. 어떤 웹 서버와도 마찬가지로, 연결 자체는 사용자의 IP 주소, 브라우저 식별 문자열, 시각, 요청한 파일 이름을 함께 실어 보냅니다. 그 도메인은 정적 파일 호스팅(Cloudflare)일 뿐이고, 저희는 그 위에 저희 코드를 올려 두지 않았으며, **요청 단위의 접근 기록을 어디로도 내보내거나 보관하지 않습니다**. 저희가 볼 수 있는 것은 서비스 제공업체의 집계 트래픽 통계(총 요청 수, 캐시 적중률 같은 것)뿐이며, 그 안에 개별 사용자는 없습니다. IP 주소는 여전히 전송과 남용 방지 과정에서 제공업체가 처리하며, 그것은 연결 자체에서 피할 수 없는 일입니다. 상위 원본인 `easylist.to`로 되돌아갈 때 사용자의 IP를 보는 것은 EasyList의 인프라(제3자)이며, 저희 손에 있지 않습니다.

**(2) 잠금 화면의 아트워크 → 해당 웹페이지가 지정한 이미지 출처**
미디어를 재생할 때 잠금 화면에 아트워크를 표시해야 합니다. 그 이미지의 URL은 웹페이지 자신의 metadata에서 나오며, 보통은 제3자 이미지 호스팅이나 CDN입니다. **이것은 사용자가 페이지를 직접 열어 두지 않은 상태에서도 발생하는 유일한 제3자 연결입니다** — 앱이 백그라운드에 있고 화면이 잠겨 있을 때에도 이 연결은 나갑니다. 저희는 이것을 최소한으로 제한했습니다. cookie를 싣지 않고 캐시를 쓰지 않는 독립된 네트워크 설정을 사용하며, **비공개 탭은 아트워크를 전혀 가져오지 않고 잠금 화면에도 어떤 정보도 보내지 않습니다**. 완전히 끄려면 설정 → ‘미디어’ → ‘잠금 화면 제어’를 끄십시오. 그 스위치는 잠금 화면과 제어 센터의 곡목 정보 및 재생 제어도 함께 비활성화한다는 점에 유의하십시오.

**(3) 피드 구독(RSS/Atom) → 사용자가 탭한 그 피드 URL**
주소 표시줄의 피드 아이콘을 탭할 때 발생합니다. 피드 페이지는 **평범한 탭**이므로, 새로 고치거나 다음번에 앱을 실행해 그 탭을 복원할 때 다시 한 번 가져옵니다. 독립된 네트워크 설정을 사용하며, cookie를 싣지 않고 캐시를 쓰지 않고, 내용은 메모리에서만 해석되어 디스크에 남지 않습니다.

**(4) 리더 화면 안의 이미지 → 기사의 출처와 그 이미지 호스팅**
리더로 다시 배치한 뒤에도 원문의 이미지는 계속 불러옵니다. 이 화면에는 해당 탭과 동일한 차단 규칙이 적용되며, 해당 탭 자신의 저장소를 사용합니다(그래서 비공개 탭의 리더 역시 디스크에 남지 않습니다).

**(5) 앱 내 구입 → Apple(StoreKit)**
구입하거나 구입을 복원할 때에만 발생합니다. 오가는 상대는 Apple이지 저희가 아닙니다. 거래 기록은 StoreKit(Apple의 시스템 구성 요소)이 보관하며, **저희는 사용자의 기기에 따로 복사본을 두지 않고**, 복사해 둘 수 있는 서버도 없습니다 — 앱은 실행할 때와 구입한 뒤에 시스템에 ‘이 Apple ID에 지금 유효한 구독이 있는가’를 한 번 물을 뿐이고, 돌려받는 것은 있음/없음과 만료일입니다.

**(6) 사기 웹사이트 경고 → Apple**
iOS에 내장된 웹 엔진이 사용자가 방문하는 URL에 대해 알려진 사기 사이트나 악성 사이트인지를 Apple에 조회하고, 해당하면 차단합니다. **이것은 iOS의 시스템 차원 기능이며, 조회 대상은 Apple이고, 저희는 이를 볼 수도 건드릴 수도 없습니다.** 저희는 이 기능을 켜 둔 채로 두기로 했습니다 — 끄면 이 처리방침에서 한 항목을 덜어낼 수 있지만, 그만큼 보호가 한 겹 줄어듭니다.

**(선택) 문제 신고**: 설정 → 정보 → ‘문제 신고’를 누르면 **사용자 본인의** 메일 앱(또는 공유 시트)이 열리며, 수신자는 저희 지원 메일 주소입니다. 앱이 스스로 어떤 데이터도 전송하지 않습니다 — 본문과 첨부 파일은 보내기 전에 전부 볼 수 있고, 보내기를 누르는 것은 사용자입니다. 사용자가 쓴 설명 외에 메일에는 세 줄이 미리 채워집니다. 앱 버전, iOS 버전, 기기 모델(예: ‘iPhone’) — 일련번호도 없고 어떤 식별자도 없으며, 보내기 전에 지울 수 있습니다.

첨부 파일은 ‘진단 로그’(설정 → 개인정보 보호, **기본값은 꺼짐**)이며, 기능 이벤트(재생, 차단, 전체 화면 등)만 기록합니다. URL은 **디스크에 기록되는 그 시점에** 이미 가려지며, 보낼 때 처리하는 것이 아닙니다.

- 일반 탭: `https://호스트`만 남고, 경로와 쿼리 문자열은 모두 ‘…’으로 바뀝니다.
- **비공개 탭: 호스트조차 남지 않고**, URL 전체가 ‘‹私密›’으로 바뀝니다(이 표기는 로그에 그대로 기록되는 고정 문자열입니다).
- URL 안에 아이디와 비밀번호가 들어 있으면(`https://아이디:비밀번호@…` 같은 형태), 가리기 전에 먼저 제거됩니다.

스위치를 끄면 기록 전체가 삭제됩니다. 저희가 받은 신고는 디버깅에만 사용하며, 다른 어떤 데이터와도 연결하지 않고, 제3자에게 제공하지 않으며, 처리가 끝난 뒤 90일 이내에 삭제합니다.

위에 적은 것 외에 앱은 다른 외부 연결을 하지 않습니다.

---

## 4. 비공개 탭

**하는 것:**

- 방문 기록에 쓰지 않고, 탭 목록에도 쓰지 않으며, 앱을 다시 열어도 복원하지 않습니다.
- 웹사이트 데이터(cookie, 캐시)는 메모리에만 두며, 닫으면 사라집니다.
- 리더도 같은 ‘메모리 전용’ 저장소를 사용합니다.
- 제목이나 아트워크를 잠금 화면, 제어 센터, 차량용 화면에 보내지 않습니다.
- 재생 대기열은 메모리에만 남기며, 디스크에 쓰지 않습니다.

**사용자가 직접 한 동작은 여전히 흔적을 남깁니다.** 비공개 탭에서 북마크를 추가하거나 읽기 목록에 저장하면, 그 URL은 해당 목록에 기록됩니다. 이것은 의도적입니다 — 사용자가 명시적으로 저장을 요청한 것을 저희가 몰래 버리지는 않습니다. 다만 알아 두실 만한 일입니다.

---

## 5. 저희가 웹페이지에 하는 일

Vigatamala는 콘텐츠 차단형 브라우저입니다. 차단을 위해 저희는 다음과 같은 일을 합니다.

- 규칙에 따라 네트워크 요청을 차단하고 페이지 요소를 숨깁니다.
- 광고 영역을 제거하기 위해 페이지 자신의 데이터 요청을 가로챕니다(이는 사이트가 보내는 응답 내용의 일부를 저희가 읽고 고쳐 쓴다는 뜻입니다).
- 팝업 창과 뒤로 가기 하이재킹을 가로챕니다.
- 사이트에 노이즈를 섞은 브라우저 핑거프린트를 보고합니다(사이트 간에 식별될 가능성을 낮춥니다).
- Global Privacy Control 신호를 보냅니다.
- cookie 동의 배너를 자동으로 닫습니다 — **항상 거부를 선택합니다**. 일부 사이트의 동의 플랫폼은 이 ‘거부’ 결정을 자기 서버에 보고하는데, 그것은 그 플랫폼의 동작이지 저희가 시작한 일이 아닙니다.
- ‘앱으로 열기’ 권유 벽을 자동으로 닫습니다 — ‘나중에’ 같은 부정 선택지를 찾을 수 있으면 **사용자를 대신해 눌러 주고**, 찾지 못하면 그 오버레이만 숨깁니다. **권유 버튼은 절대 누르지 않으며**, 비밀번호 입력란이 있는 오버레이(로그인 벽)에는 손대지 않습니다. 위와 마찬가지로, 눌린 그 선택지 때문에 사이트가 ‘이 사람은 아니요를 골랐다’고 기록한다면 그것은 그 사이트의 동작입니다.
- 요청을 보내기 전에 URL을 고쳐 씁니다. 추적에만 쓰이는 파라미터를 떼어내고, Google의 AMP 껍데기를 원래 페이지로 되돌리며(이 항목은 호스트가 바뀌는데, 도착지는 그 링크가 원래 가리키던 바로 그 페이지입니다), http를 https로 올립니다(승격에 실패하면 조용히 되돌리지 않고 사용자에게 물어봅니다).
- Safari와 동일한 User-Agent 문자열을 보냅니다(사이트가 이 브라우저를 알아보지 못해서 축소된 페이지를 내주는 일이 없도록 하기 위함입니다).
- 사용자가 야간 모드를 켜면 밝은 웹페이지에 색 반전을 적용합니다.

**User-Agent를 제외하면 위의 모든 항목은 전역으로 끌 수 있으며**, 대부분은 주소 표시줄의 방패 메뉴에서 개별 사이트별로도 끌 수 있습니다(차단, 트래커 차단, 남은 광고 영역 제거, 하이재킹 방지, 핑거프린팅 보호, HTTPS 강제, 쿠키 안내, 야간 모드, 데스크톱 사이트). 두 가지 예외는 분명히 밝혀 두겠습니다.

- **User-Agent에는 끄는 선택지가 없습니다.** 앱은 언제나 Safari와 동일한 문자열로 자기를 소개합니다(방패 메뉴의 ‘데스크톱 사이트’는 macOS Safari 버전으로 바꿔 줄 뿐, 이 기능을 끄는 것이 아닙니다). 이것은 의도적입니다. 알아볼 수 없는 브라우저 문자열은 일부 사이트에서 아예 서비스 거부를 당하거나 축소된 페이지를 받게 만들며, 그리고 **Safari와 동일한 문자열**이 내보내는 식별 정보는 사실 **WKWebView의 기본 문자열보다 적습니다** — 이 문자열은 사용자를 ‘어떤 소수 브라우저의 사용자’가 아니라 다른 모든 Safari 사용자와 똑같아 보이게 합니다.
- **Global Privacy Control에는 전역 스위치만 있습니다**(이것은 모든 사이트에 일관되게 적용되는 법적 신호이므로, 사이트마다 보내거나 말거나 하는 것은 오히려 의미가 없습니다). 기술적 한계도 함께 밝힙니다. `Sec-GPC` 헤더는 앱이 스스로 보내는 메인 프레임 요청에만 붙일 수 있는데, iOS의 웹 엔진은 페이지 안의 하위 리소스 요청에 헤더를 추가할 방법을 제공하지 않기 때문입니다. `navigator.globalPrivacyControl`은 페이지 전체에 대해 성립합니다.

---

## 6. 저희가 하지 않는 일

- 사용자의 어떤 데이터도 판매하거나 공유하거나 임대하지 않습니다 — 애초에 저희에게는 사용자의 데이터가 없습니다.
- 광고, 제휴 링크, 협찬 콘텐츠를 끼워 넣지 않습니다.
- 앱이나 웹사이트를 넘나드는 사용자의 행동을 추적하지 않습니다.
- 콘텐츠 다운로드나 오프라인 저장 기능을 제공하지 않습니다.

---

## 7. 아동

이 앱은 만 13세 미만 아동을 대상으로 하지 않으며, 아동의 개인정보를 알면서 수집하지 않습니다.

이 앱의 App Store 연령 등급은 **16+** 입니다. 이는 범용 브라우저이기 때문에 따라오는 필연적인 결과입니다 — 이 앱은 어떤 웹페이지든 열 수 있고(Apple 설문의 ‘제한 없는 웹 접근’), 그 항목의 최저 등급이 바로 16+ 이며, 모든 주요 브라우저가 마찬가지입니다.

---

## 8. 앞으로의 변경

저희가 계정 백엔드를 연결하거나, 기기 간 동기화 또는 학생 인증을 도입하면 수집하는 데이터가 달라집니다. 그때가 되면 이 처리방침을 갱신하고 앱 안에서 알려 드리겠습니다. **현재 이 기능들은 모두 아직 제공되지 않습니다.**

---

## 9. 문의

문의 사항은 다음 주소로 보내 주십시오: **vigatamala@link2us.link**

개발자: **Su Shih-neng**
